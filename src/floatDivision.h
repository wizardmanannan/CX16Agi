#include "general.h"
#include "memoryManager.h"
#include "fixed.h"
#include "helpers.h"

void b6InitFloatDivision();

extern fix32 floatDivision(byte numerator, byte denominator);

extern fix32 DIV(int numerator, int denominator);
extern int round(fix32 aNumber, boolean isPos);
